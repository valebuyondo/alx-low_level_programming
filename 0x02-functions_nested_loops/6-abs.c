#include "main.h"
/**
 *_abs - computes the absolute value of an integer.
 *@r: the integer to be computed
 *Return: returns -r if r is greter thareturns r otherwise
 */
int _abs(int r)
{
	if (r < 0)
	{
		return (-r);
	}
	return (r);
}
