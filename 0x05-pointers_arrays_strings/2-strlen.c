#include "main.h"
#include <string.h>
/**
 *_strlen - the unction
 *@str: string to be checked
 *Return: return the length of the string
 */
int _strlen(char *str)
{
	int len;

	len = strlen(str);
	return (len);
}
