#include <stdio.h>
/**
 * main - prints a name of the function
 * @argc: counts the number of parameters in a funtion
 * @argv: argument vector
 * Return: always 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
